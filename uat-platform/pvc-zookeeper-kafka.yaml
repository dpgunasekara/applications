apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: bitnami-data
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: azurefile-csi
  volumeName: bitnami-data
  resources:
    requests:
      storage: 5Gi
