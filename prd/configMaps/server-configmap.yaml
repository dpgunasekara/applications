apiVersion: v1
kind: ConfigMap
metadata:
  name: server-config
data:
  server.yaml: |
    server:
      ssl:
        key-store-type: PKCS12
        key-store: /etc/ssl/certs/bh-bob-bw.p12
        key-store-password: ${KEY_STORE_PASSWORD}
        key-store-alias: bh.bob.bw
        enabled: true
    management:
      endpoint:
        health:
          show-details: always
          probes:
            enabled: true
      health:
        livenessState:
          enabled: true
        readinessState:
          enabled: true